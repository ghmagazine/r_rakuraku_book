# 20章 おわりに

## 学ぶことをおすすめする内容

### インポート

# 紹介したパッケージ
## Webスクレイピング
install.packages("rvest") #静的なスクレイピング
install.packages("rselenium") #ブラウザの操作

## GoogleなどのWebサービスからの情報の取得
install.packages("googledrive")　#グーグルドライブからのデータ取得
install.packages("googlesheets4")　#グーグルシートからのデータ取得

### 可視化

install.packages("ggplot2")

### 表の作成

install.packages("gtsummary") #集計表の作成
install.packages("gt") #表の作成
install.packages("flextable") #表の作成

### 統計分析

### レポート作成

install.packages("officer") #ワード、パワポへの出力

### プログラミング言語としてのR

install.packages("shiny") # ダッシュボードツールの作成
